{
  "$schema": "https://opencode.ai/config.json",
  "username": "Lin", // 用户名，AI在对话中的称呼
  "default_agent": "lin", // 启动默认激活的agent，对应下面agent列表中寻找。
  "mcp": {
    "agent-lin": {
      "type": "local",
      "command": ["python", "mcp-servers/agent-lin/server.py"],
      "enabled": true,
      "env": {
        "TAVILY_API_KEY": "${TAVILY_API_KEY}"
      }
    }
  },
  "agent": {
    "lin": {
      "description": "Lin 的个人助手 - 支持搜索问答、以图搜图、网页爬取和文件操作",
      "mode": "primary",
      "permission": {
        "read": "allow",
        "edit": "allow",
        "bash": "allow",
        "glob": "allow",
        "grep": "allow"
      }
    }
  }
}
